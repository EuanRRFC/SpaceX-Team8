<Window x:Class="Star_Reader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Star_Reader"
        xmlns:dragablz="clr-namespace:Dragablz;assembly=Dragablz"
        xmlns:dockablz="clr-namespace:Dragablz.Dockablz;assembly=Dragablz"
        xmlns:system="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="Star Reader" Height="480" Width="720">
    <Window.Resources>
        <system:String x:Key="Partition">QuickStart</system:String>
    </Window.Resources>
    <dockablz:Layout Partition="{StaticResource Partition}">
        <dragablz:TabablzControl Margin="8" ConsolidateOrphanedItems="True" >
            <dragablz:TabablzControl.InterTabController>
                <dragablz:InterTabController Partition="{StaticResource Partition}" />
            </dragablz:TabablzControl.InterTabController>
            <TabItem Header="File Upload" IsSelected="True">
                <Grid>
                    <StackPanel  Orientation = "Horizontal" VerticalAlignment="Top">
                        <Button x:Name="UploadFileButton" x:FieldModifier="public" Click="ReadFile" Content="Upload File" Margin="10" Height="20" Width="74"/>
                        <Button x:Name="OpenInTabView" x:FieldModifier="public" Click="OpenTab" Content="Open as tab" Margin="10" Height="20"  Width="74"/>
                    </StackPanel>
                    <TextBox x:Name="FileContentBox" x:FieldModifier="public" Margin="10,40,10,5" TextWrapping="Wrap" Text="TextBox"/>
                </Grid>
            </TabItem>
            <TabItem Header="Tab No. 2">
                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">We Have Tearable Tabs!</TextBlock>
            </TabItem>
            <TabItem>
                <!-- context menu -->
                <TabItem.Header>
                    <Grid>
                        <Grid.ContextMenu>
                            <ContextMenu>
                                
                                <MenuItem Command="{x:Static dragablz:TabablzControl.CloseItemCommand}"/>
                            </ContextMenu>
                        </Grid.ContextMenu>
                        <TextBlock>Tab No. 3</TextBlock>
                       
                    </Grid>
                </TabItem.Header>
                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">I feel like an ice cold drink</TextBlock>
     
            </TabItem>
            <TabItem Header="Tab No. 4">
                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">We Have Tearable Tabs!</TextBlock>
            </TabItem>
        </dragablz:TabablzControl>
    </dockablz:Layout>
</Window>
